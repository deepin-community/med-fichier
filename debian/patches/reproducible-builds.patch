Index: med-fichier/doc/dox/Doxyfile.cfg.cmake.in
===================================================================
--- med-fichier.orig/doc/dox/Doxyfile.cfg.cmake.in
+++ med-fichier/doc/dox/Doxyfile.cfg.cmake.in
@@ -139,7 +139,8 @@ STRIP_FROM_INC_PATH    =
 # (but less readable) file names. This can be useful is your file systems
 # doesn't support long names like on DOS, Mac, or CD-ROM.
 
-SHORT_NAMES            = YES
+# Disable short names to improve reproducibility
+SHORT_NAMES            = NO
 
 # If the JAVADOC_AUTOBRIEF tag is set to YES then Doxygen
 # will interpret the first line (until the first dot) of a JavaDoc-style
Index: med-fichier/doc/dox/Doxyfile.cfg.in
===================================================================
--- med-fichier.orig/doc/dox/Doxyfile.cfg.in
+++ med-fichier/doc/dox/Doxyfile.cfg.in
@@ -178,7 +178,8 @@ STRIP_FROM_INC_PATH    =
 # support long names like on DOS, Mac, or CD-ROM.
 # The default value is: NO.
 
-SHORT_NAMES            = YES
+# Disable short names to improve reproducibility
+SHORT_NAMES            = NO
 
 # If the JAVADOC_AUTOBRIEF tag is set to YES then doxygen will interpret the
 # first line (until the first dot) of a Javadoc-style comment as the brief
